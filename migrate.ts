import { db, pool } from './src/db/client.ts'; import fs from 'fs'; async function up() { const sql = fs.readFileSync('drizzle/0003_dry_silhouette.sql', 'utf8'); const stmts = sql.split('--> statement-breakpoint'); for(const stmt of stmts) { if (stmt.trim()) { console.log('Executing:', stmt.trim()); await db.execute(stmt.trim()); } } console.log('Done!'); pool.end(); } up().catch(e => { console.error(e); pool.end(); });
